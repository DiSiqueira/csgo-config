exec ludo237/xair/scream
exec ludo237/binds
exec ludo237/connection
exec ludo237/hud
exec ludo237/input
exec ludo237/monitoring
exec ludo237/pov
exec ludo237/sound


echo ".:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:."
echo ".                           Other commands                            ."
echo ".:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:._.:*~*:."

// Use key won't open buymenu, set to 1 for default behaviour
cl_use_opens_buy_menu "0"

// Set to 1 for the buymenu to close after every bought item
closeonbuy "0"

// Filter damage reports in console
con_filter_text damage

// 1 = Only damage reports are written in console; 2 = damage reports are highlighted
con_filter_enable "2"

// Remove the slashes at to get a clean output of damage done and damage recieved in the
developer "1"

// top left corner after you die.
con_filter_text_out "FFS:"

// Some other commands, dunno if they works
// @untested
adsp_debug "0"
ai_report_task_timings_on_limit "0"
ai_think_limit_label "0"